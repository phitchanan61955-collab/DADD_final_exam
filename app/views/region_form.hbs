<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{title}}</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/styles.css" rel="stylesheet">
</head>

<body class="bg-light">

  <!-- Top bar -->
  <nav class="navbar navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand fw-semibold" href="/admin">Admin Panel</a>
      <div class="d-flex gap-2">
        <a href="/regions" class="btn btn-outline-light btn-sm">Back to Region List</a>
      </div>
    </div>
  </nav>

  <div class="container py-4" style="max-width: 900px;">

    <div class="mb-3">
      <h1 class="h4 mb-1">Add Region</h1>
      <div class="text-muted small">Create a new region (master data).</div>
    </div>

    <div class="card border-0 shadow-sm">
      <div class="card-body p-4">

        <form method="post" action="/regions/add" class="row g-3">
          <div class="col-12">
            <label class="form-label fw-semibold">Region Name</label>
            <input
              type="text"
              name="region_name"
              class="form-control"
              placeholder="e.g., Asia"
              required
            />
          </div>

          <div class="col-12 d-flex gap-2 mt-2">
            <button type="submit" class="btn btn-primary">
              Create
            </button>
            <a href="/regions" class="btn btn-outline-secondary">
              Cancel
            </a>
          </div>
        </form>

      </div>
    </div>

  </div>

</body>
</html>
