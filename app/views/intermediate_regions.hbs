<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>{{title}}</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/styles.css" rel="stylesheet">
</head>

<body class="bg-light">

  <!-- Top bar -->
  <nav class="navbar navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand fw-semibold" href="/admin">Admin Panel</a>
      <div class="d-flex gap-2">
        <a href="/subregions" class="btn btn-outline-light btn-sm">Back to Sub-Region List</a>
      </div>
    </div>
  </nav>

  <div class="container py-4" style="max-width: 900px;">

    <div class="mb-3">
      <h1 class="h4 mb-1">Add Sub-Region</h1>
      <div class="text-muted small">Create a new sub-region and assign it to a region.</div>
    </div>

    <div class="card border-0 shadow-sm">
      <div class="card-body p-4">

        <form method="post" action="/subregions/add" class="row g-3">

          <div class="col-12">
            <label class="form-label fw-semibold">Sub-Region Name</label>
            <input
              type="text"
              name="sub_region_name"
              class="form-control"
              placeholder="e.g., Eastern Asia"
              required
            />
            <div class="form-text">Use a clear and consistent name.</div>
          </div>

          <div class="col-12">
            <label class="form-label fw-semibold">Region</label>
            <select name="region_id" class="form-select" required>
              <option value="">-- Select Region --</option>
              {{#each regions}}
                <option value="{{this.region_id}}">{{this.region_name}}</option>
              {{/each}}
            </select>
          </div>

          <div class="col-12 d-flex gap-2 mt-2">
            <button type="submit" class="btn btn-primary">
              Create
            </button>
            <a href="/subregions" class="btn btn-outline-secondary">
              Cancel
            </a>
          </div>

        </form>

      </div>
    </div>

  </div>

</body>
</html>
